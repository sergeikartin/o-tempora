# Config Variables

<!-- Configuration variables reference (env vars, feature flags, tunable constants). Empty until the project has runtime config worth documenting separately from CLAUDE-decisions.md's architecture description. -->
