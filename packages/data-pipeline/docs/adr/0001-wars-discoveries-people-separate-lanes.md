---
status: accepted
---

# Wars/Discoveries and People stay separate lanes end-to-end

Wars & Conflicts (`wars.json`, `War[]`) and Discoveries & Inventions (`discoveries.json`, `Discovery[]`) each get their own fetch/score/tag/output path — `War` and `Discovery` both extend `TimelineEntry`
