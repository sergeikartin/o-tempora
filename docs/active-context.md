# Active Context

Update this file after every meaningful implementation change. Keep entries high-level — what was built and what decisions matter going forward, not a full implementation narrative. Detailed history remains available via `git log`.

## Current Phase

- Frontend scaffold

## Current Goal

- None. `packages/web` typechecks and renders all three lanes (People, Wars & Conflicts, Events & Inventions) with real published data, now on D3 — see Next Up for remaining priority ordering (ticket 05 vs. Unit 9).

## Completed

- Monorepo restructure: pipeline, shared-types, and frontend under `packages/*` as npm workspaces.
- Units 1–4: data pipeline scaffold (Fetch/Score/Tag/Output stages, `packages/shared-types`), frontend scaffold (`packages/web`, Vite + React 19 + TS, mini-FSD, Steiger boundaries), independently-scrolling synced `vis-timeline` lanes.
- Wars & Conflicts lane added: ranges/points, battle/treaty→war membership, ruler reign-period overlays (`Person.reignPeriods`).
- Fame Tier Redesign: sitelink/HPI floors replaced top-N fame-tier ceilings, applied across all three lanes; later split per-lane (`FAME_TIER_MIN_SITELINKS_WARS`/`_DISCOVERIES`) to balance dataset sizes. (Spec lived at `context/specs/06-fame-tier-redesign.md`, since removed along with the rest of that directory — the project now specs new work under `.scratch/<feature-slug>/`, see `docs/agents/issue-tracker.md`.)
- Output stage writes to a pipeline-owned `data/output/`, published to `packages/shared-types` via `npm run publish-data` (ticket 22).
- People lane migrated from Wikidata to Pantheon 2.0 (ticket 17), with descriptions via secondary Wikidata lookups and a CC BY-SA 4.0 data-license notice; reign-period enrichment rewired onto Pantheon's retained `wd_id` (ticket 19).
- Wikidata reliability fixes (retry on 502/503/504, era-bucketed fetch with per-bucket try/catch) for Discoveries & Inventions (ticket 20) and Wars & Conflicts (ticket 25); both lanes live re-fetched and published (`discoveries.json`, `wars.json`).
- Wars/Discoveries frontend rendering restored against the new `War`/`Discovery` types (`map-to-items.ts`, `App.tsx`).
- Reign-period query fixes: restricted to genuine Head-of-State/Government positions and excluded "emeritus" titles by label; `ReignPeriod.title` threaded through to frontend tooltips.
- `.scratch/alt-data-sources/` map closed — all fetch-stage source decisions (tickets 17–25) implemented; see the map and its `issues/`/`research/` subdirectories for the full decision trail.
- Removed the stale, pre-split `packages/shared-types/src/data/events.json`, fully superseded by `wars.json`+`discoveries.json`.
- `.scratch/timeline-rendering-foundation/` map closed — the vis-timeline-vs-D3 question that paused Unit 5's grilling session (below) is decided: **D3**, for all three lanes, via a real People-lane prototype (`/prototype`) compared side-by-side against vis-timeline, then `/grilling` + `/domain-modeling` to record `packages/web/docs/adr/0001-d3-over-vis-timeline.md`. Migration execution is a new, separate, not-yet-specced follow-on effort — see Next Up.
- D3 timeline migration (`.scratch/d3-timeline-migration/`, tickets 01–04 + 06; ticket 05 still open, see Next Up) landed: `TimelineCanvas.tsx` is now one shared-horizontal-scroll container holding `PeopleLane`/`WarsLane`/`EventsLane` (new files) stacked vertically, each with its own independent `overflow-y` region — the native scroll position on that one container *is* the cross-lane time-axis sync, replacing the old three-`Timeline`-instances-plus-`rangechange`-listener-plus-reentrancy-guard design entirely. `options.ts` now holds the shared `xScale`/pixels-per-year zoom math (bounded by the existing `ZOOM_MIN_YEARS`/`ZOOM_MAX_YEARS`) and provisional inlined `DOMAIN_COLORS`/`CATEGORY_COLORS`; `map-to-items.ts` is pure `Person`/`War`/`Discovery` → render-item mapping plus the shared `assignRows` greedy interval-graph row-stacking function (used by People and Wars & Conflicts; Events & Inventions is points-only, single row). Years are plain numbers end-to-end now — `shared/lib/dates.ts` dropped `toLegacyDate`/`yearToPlainDate` (nothing needs a legacy `Date` anymore) and keeps only `today()`. `vis-timeline` is removed from `packages/web/package.json`; `d3` added. Zoom's center-preserving re-pan (recenters on the year at the viewport's middle before a +/- click) is new behavior, not a port. Docs updated: `packages/web/docs/code-conventions.md`'s Timeline Rendering section, `packages/web/CLAUDE.md`'s Stack table + description line.
- Fame Tier zoom + century Year Axis headers (`.scratch/frame-tier-zoom/`, tickets 01–04) landed, closing out Unit 9 by superseding it outright (see `packages/web/docs/adr/0002-fame-tier-drives-zoom.md`, since itself superseded — see below): the data-pipeline's `scoreAndRank`/`scoreAndRankDiscoveries`/`scoreAndRankByHpi` (`packages/data-pipeline/src/transform/score.ts`) now floor at `specialist` instead of `generalPublic`, and `wars.json`/`discoveries.json`/`people.json` were rebuilt and republished against that wider floor (People: 3,680 rows, up from the old `generalPublic`-only count). `ZOOM_MAX_YEARS` widened 250 → 500 (`shared/config/viewport.ts`). Separately, a new shared `shared/lib/format-year.ts` (`formatYear`, `centuryBoundariesInRange`) renders every user-facing year as "N BCE"/"N CE" (ISO/astronomical numbering — year 0 is 1 BCE), used by `YearAxis`'s tick labels, `mapPeople`'s tooltip (including its reign-period sub-tooltip), and a new major century-header row above `YearAxis`'s existing minor-tick row (retuned to a `TARGET_TICK_SPACING_PX` of 70, targeting the 60-80px band d3's "nice"-step snapping oscillates around). `mapWars`/`mapDiscoveries` tooltips don't currently interpolate a year, so ticket 03's original expectation that they would didn't hold against the actual code — nothing changed there. In passing, fixed a real (pre-existing, this-session-introduced) bug from the "Move data-pipeline source into src/" restructure: `RAW_DIR`/`DATA_DIR`/`SOURCE_DIR`/`DEST_DIR` path constants across `fetch/`/`transform/`/`output/` were still computed with the pre-move relative `..` depth, silently pointing at a nonexistent `src/data/` instead of the package-root `data/`; `build-data`/`publish-data` would not run at all until this was fixed. Docs updated: `packages/web/docs/code-conventions.md`'s Timeline Rendering + File Organization sections (also dropped the now-superseded `features/filter-by-fame-tier` slice from the planned file layout).
- Sidebar with Legend + manual Fame filters (`.scratch/sidebar-filters-legend/`) landed, superseding the zoom-coupled Fame Tier system outright (`packages/web/docs/adr/0003-manual-fame-filter-replaces-zoom-tier.md`; ADR 0002 marked superseded). A new always-visible `widgets/sidebar` renders a Legend (static pills for People's 8 `OccupationDomain` colors, moved to a new shared `shared/config/occupation-domain-colors.ts` since both `widgets/timeline-canvas` and `widgets/sidebar` need it) and Filters (one raw-`fameScore` numeric floor per lane, via a new `features/filter-by-fame-score` slice — `model/useFameScoreFilters` owns session-only state, called once in `App.tsx` and threaded as props to both `Sidebar` and `TimelineCanvas`; `ui/FameScoreFilters` is the presentational number-input row). Zoom no longer affects entity density at all — `TimelineCanvas.tsx` takes the feature's `FameScoreValues` as one prop instead of deriving a tier from `pixelsPerYear`; `shared/config/viewport.ts` drops the `FameTierName`/`FAME_TIER_*` tables in favor of a single `FAME_SCORE_BOUNDS: Record<FameScoreLane, {min,max,default}>` table (defaults match the old CORE tier so first paint is unchanged), and `options.ts` drops `fameTierForViewport`/`fameTierForVisibleYears`. The read-only `fameTierIndicator` next to the zoom buttons is gone with nothing replacing it. `app/global.css` gained a `:root` block wiring `design-tokens.md`'s color/radius tokens as CSS custom properties (not yet typography — no webfont loaded) — the first real consumer of those tokens outside `widgets/timeline-canvas`'s still-provisional-hex D3 core, though `DOMAIN_COLORS`' per-domain hex itself stays provisionally inlined (ticket 05) same as before, just relocated to `shared/config`. This was implemented directly rather than through the map's originally-scoped `/prototype` + `/grilling` tickets — both are `Status: resolved` in `.scratch/sidebar-filters-legend/issues/`, but via a `## Answer` recording that the user redirected past both sessions to `/implement`, not by actually running them: control type (plain number input per lane, not slider/stepper), bounds/step, and legend pill treatment (color swatch + label only, no count) were decided inline during implementation instead; flagged here since it departs from the map's stated process.
- Events & Inventions lane switched from a Wikidata-SPARQL-candidate scan to a hand-curated 121-event source (`.scratch/events-inventions-curated-source/` map closed, all 9 tickets resolved) — the old `inventions.ts` P575-only query produced junk top entries ("Brazil"/"Antarctica", no real classification signal). `packages/data-pipeline/data/raw/events-curated.raw.json` (checked in, curator-verified `name`/`year`/`category`/`description`) is now ground truth; a new Fetch step (`fetch-events-enrichment.ts` + `queries/events-enrichment.ts`) batches a per-QID SPARQL pass (mirroring the People-lane's descriptions/reigns pattern) to backfill `sitelinks`/`wikipediaUrl`/`country`, writing `data/raw/events-curated-enriched.raw.json`. `Category` (Wars' type) dropped `"invention"`; a new disjoint `DiscoveryCategory` (10 values, `packages/shared-types`) replaces it for `Discovery.category`, tagged by straight passthrough (`tagCuratedDiscovery`) rather than a lookup table. Score dropped Discoveries' specialist-floor filter entirely (`rankDiscoveriesBySitelinks`, sort-only, no `FAME_TIER_MIN_SITELINKS_DISCOVERIES`) — the ~121-item curated set is already hand-vetted, so the sidebar's user-facing fame-score floor is now this lane's only density control (`FAME_SCORE_BOUNDS.discoveries` recalibrated to `{min:8, max:296, default:25}` from the real published range). A new `DISCOVERY_CATEGORY_COLORS` palette (`options.ts`) was locked with the user (hue-optimized against all 15 existing People/Wars colors, matched to the app's pastel family) and wired into `EventsLane.tsx`. Old `inventions.ts`/`inventions.test.ts`/`events-inventions.raw.json` (~170K lines) deleted; `fetch-events.ts` renamed `fetch-historical-events.ts` (now Wars-only). Live run against Wikidata: 121/121 curated QIDs enriched successfully, 0 rows dropped at Output — `discoveries.json` regenerated and published. Docs updated: `packages/data-pipeline/CLAUDE.md`'s overview/Stack/Data-Pipeline sections.

- Time-period data model refactor: split each entity's dates into a `Period` (`{start, end?}`, a real duration — `Person.lifespan`, `War.period`, `ReignPeriod`) vs. a `PointInTime` (`{at}`, a single moment — `Discovery`, and the Wars & Conflicts lane's non-war entries), both built from a shared `YearMonth` (`{year, month?}`) — all new types in `packages/shared-types/src/index.ts`. Wars & Conflicts' single `War` type split into two: `War` (always a real Period, only entities Wikidata classes as wd:Q198) and a new `WarEvent` (always a PointInTime — battles, treaties, sieges, revolutions, rebellions, military operations, generic historical events); `wars.json` still ships one array, now typed `WarsAndConflictsEntry = War | WarEvent`, narrowed structurally (`"period" in entry`) rather than via a `kind` tag, since the two shapes are already disjoint. Month granularity wired end-to-end, not just the type shape: Pantheon's already-downloaded `birthdate`/`deathdate` CSV columns are now parsed for People (`pantheon-row-shape.ts`, dropping the month when that column's own year disagrees with `birthyear`/`deathyear` — the two occasionally disagree, e.g. a Julian/Gregorian calendar-boundary date); Wikidata's `wikibase:timePrecision` was added to the Wars & Conflicts (`historical-events.ts`) and reign-period (`reigns.ts`) SPARQL queries via the full `p:`/`psv:`/`pqv:` statement/value-node model (month is only trusted at precision >= 10 — Wikidata canonicalizes an unknown month/day to "01", which a bare wdt:/pq: truthy read can't distinguish from a real January 1st). Discoveries stay year-only (the hand-curated source has no month field). All three raw datasets re-fetched/rebuilt/republished live against Wikidata + Pantheon. Frontend: `shared/lib/format-year.ts` gained `formatYearMonth` (hardcoded month-name lookup, not `Temporal.PlainYearMonth#toLocaleString` — the polyfill's locale path needs an explicit calendar-qualified locale to avoid throwing, and buys nothing extra over a plain 1-12 lookup here); `map-to-items.ts`'s `mapPeople` tooltip (lifespan + reign-period sub-tooltips) is the only place month actually renders — `mapWars`/`mapDiscoveries` tooltips still don't interpolate any date, unchanged. Positioning/pixel math is untouched throughout (still plain per-year numbers) — this was explicitly scoped as tooltip-only, no visual changes. Docs updated: `packages/web/docs/code-conventions.md`'s Timeline Rendering section.

## In Progress

- Initial-load performance: profiling (`performance_start_trace` against the dev server) found LCP at ~5.9s, ~99% of it "render delay" rather than network — traced to the Year Axis sizing its tick count off the *entire* ~4,776-year scrollable domain regardless of scroll position (~2s alone), plus the three lanes rendering every fame-tier-filtered item unwindowed (~1-2s) and the `assignRows` row-stacking algorithm (~0.6s, minor by comparison). The Year Axis went through two rewrites: first a single-row baseline with a fixed-step D3 join windowed to the visible scroll range (~4.1s real-measured LCP), then replaced again with a plain HTML/CSS ruler — tick marks are pure layered CSS gradients (no per-tick DOM node, no windowing needed at all) with only the decade-number labels as a small windowed positioned-`<span>` list; `TimelineCanvas.tsx`'s scroll container also gained faint full-height decade gridlines via the same CSS technique (see `packages/web/docs/code-conventions.md`'s Timeline Rendering section). Real-measured LCP holds at ~4.1-4.4s after the CSS rewrite (no regression, axis cost was already near-eliminated) — the remaining gap to the ~2.5s target is now entirely the People/Wars/Events lanes rendering their full filtered set unwindowed. A combined experiment (windowed axis + CORE-tier-only initial load) hit ~2.0s, under target, but progressive tier loading (defer lower-fame entries to on-demand) was never implemented — and its original framing (defer NOTABLE/EXHAUSTIVE as the user *zooms*) no longer applies now that zoom-coupled Fame Tier is gone (`packages/web/docs/adr/0003-manual-fame-filter-replaces-zoom-tier.md`). Next: scope and implement progressive loading keyed off the sidebar's fame-score floor instead (e.g. defer entries between the current floor and some wider default until the user raises it), as the second half of this effort.

## Next Up

- Unit 5 / D3 migration ticket 05 (`.scratch/d3-timeline-migration/issues/05-real-design-tokens.md`, blocked on Unit 5's still-open decisions below): replace the D3 rendering core's provisional inlined colors (`DOMAIN_COLORS`/`CATEGORY_COLORS`/`REIGN_STRIPE_COLOR` in `packages/web/src/widgets/timeline-canvas/options.ts`) with real CSS custom properties once `packages/web/docs/design-tokens.md`'s palette is wired as tokens; also resolve the reign-period marker's real token (tentatively `color-accent-selected`, not yet decoupled into its own `color-marker-reign-period` token) and the lifespan-bar name-label font-size/padding at the finalized 16px bar height.

## Open Questions

- `historical-events.ts`'s `EVENT_TYPES` list misses some historically-significant items typed under a more specific Wikidata subclass than the 8 listed classes — confirmed World War II and the Thirty Years' War are absent from `wars.json` for this reason (pre-existing, not a ticket-20 regression; e.g. WWII is likely typed as "world war" Q103495, not plain "war" Q198). Needs a Tag-stage/query-completeness decision, not a reliability fix.
- `Person.reignPeriods` overlays now render as a solid bottom-edge stripe (D3 migration ticket 01, `REIGN_STRIPE_COLOR` in `options.ts`) but the color is still a provisional inlined hex, not a real token — ticket 05 above.
- The Pantheon license decision (`.scratch/alt-data-sources/issues/10-pantheon-license-sharealike.md`) called for a `LICENSE-DATA.md` notice *and* an in-app attribution credit. Only the file notice exists — `packages/web` has no attribution UI. Needs a `packages/web` grilling session on placement/wording; deliberately left open when the `alt-data-sources` map closed rather than reopening it there.
- People lane's default fame-score floor (`FAME_SCORE_BOUNDS.people.default`, `packages/web/src/shared/config/viewport.ts`) is 90; eyeballing suggests 85 looks better, but not yet changed — needs a deliberate decision (and to confirm it doesn't regress the initial-load LCP work in progress above) before touching it.
- Wars lane's default fame-score floor (`FAME_SCORE_BOUNDS.wars.default`, `packages/web/src/shared/config/viewport.ts`) is 100; eyeballing suggests 80 looks better, but battles/revolutions specifically need improving first (unclear what — coverage, tagging, or ranking) before just lowering the floor. Not yet changed.

## Session Notes

- Re-run Fetch: `cd packages/data-pipeline && npm run fetch` — Pantheon (CSV download → HPI-filtered description batch → HPI-filtered reigns batch) → Wikidata historical events (Wars) → curated-events enrichment (Discoveries), in that order. To re-run just the Discoveries enrichment pass in isolation (e.g. after editing `data/raw/events-curated.raw.json` by hand): `npx tsx src/fetch/fetch-events-enrichment.ts`.
- Rebuild dataset after a Fetch re-run: `npm run build-data` (writes into the pipeline's own `data/output/`), then `npm run publish-data` to copy into `../shared-types/src/data/` (verify via `git status packages/shared-types` that only the expected files changed).
- `packages/shared-types/src/data/people.json`, `discoveries.json`, and `wars.json` are all live/published now (Pantheon ticket 17; discoveries curated-source switch, `.scratch/events-inventions-curated-source/`; wars ticket 25). The full `npm run publish-data` script is safe to run again for any future re-fetch.
- `npm install` must run from the repo root (single lockfile there).
- To extend the Wars/Discoveries region lookup table: `npx tsx src/transform/list-unmapped-countries.ts` from `packages/data-pipeline/` (People's tables are Pantheon-value-keyed and fully enumerated — no maintenance script needed).
