# 25 — Wars & Conflicts: Wikidata reliability fixes and live re-fetch

**What to build:** A live pipeline Fetch run for Wars & Conflicts completes reliably against the Wikidata Query Service and publishes a real `wars.json` — the CDB90 hybrid plan is fully abandoned (see [24 — Wars source: reopened, Wikidata-only](24-wars-source-reopen-wikidata-only.md)), so Wars stays on the existing `historical-events.ts` query, full history, no date-range carve-out.

**Blocked by:** 20 — Discoveries & Inventions: Wikidata reliability fixes. Ticket 20 lands the shared `wikidata-client.ts` fixes (502/503/504 retry with exponential backoff, raised client timeout) that this ticket reuses rather than reimplementing — both lanes are fetched via the same `fetchEvents()` function and the same client.

**Status:** done

- [x] **Already landed by ticket 20**, not redone here: `fetchEvents()`'s historical-events call is try/catch-guarded — ticket 20 scoped its `fetchBucketed()` rewrite to cover *both* of `fetchEvents()`'s query calls (historical-events and inventions), not just inventions, since they share the same function, the same client fixes, and the same per-bucket failure mode. Confirmed still true in the current code: `fetch-events.ts`'s `fetchEvents()` calls `fetchBucketed("historical events", buildHistoricalEventsQuery)`, each of the 26 buckets independently try/catch-guarded.
- [x] **Already landed by ticket 20**: `buildHistoricalEventsQuery` already takes `(limit, offset, minYear, maxYearExclusive)` and filters `?date` accordingly (`fetch/queries/historical-events.ts`), sharing the `formatYearAsSparqlDateTime` helper with `buildInventionsQuery`. `fetch-events.ts` loops the wars call over the same 26 `ERA_BUCKETS` as inventions.
- [x] Confirmed no date-range exclusion exists anywhere in the query or the bucket list — `ERA_BUCKETS` runs `-4000` through the current year, no 1600-1973 carve-out. Verified in the actual fetched output: `wars.json` has entries from year -1456 to 2026, with 442 entries falling inside the would-be CDB90 window (1600-1973) alone — no gap.
- [x] No fresh fetch execution happened under this ticket specifically — reused ticket 20's live fetch, run in the same session against the identical (already-landed) code path, rather than re-hitting WDQS a second time for what would be statistically the same data with no added verification value. `events-historical.raw.json`'s 1,208 raw rows (pre-dedup; `build-data` groups these into 771 unique wars/events) type-distribution confirms a genuine mix, not just wars: `war` (Q198) 313, `battle` (Q178561) 454, `treaty` (Q131569) 111, `siege` (Q188055) 92, `revolution` (Q10931) 49, `rebellion` (Q124734) 86, `military operation` (Q645883) 90, generic `historical event` (Q13418847) 13 — all 8 `EVENT_TYPES` represented. `build-data` output: `wars.json` 771 kept / 0 dropped. Spot-checked entries: French Revolution (1789, politics/revolution), Korean War (1950-1953, war, `partOfWarName: "Cold War"`), full-scale Russo-Ukrainian war (2022, war, `partOfWarName: "Russo-Ukrainian war"`) — all correct.
- [x] `npm run publish-data` run for real (not a manual single-file copy, since `people.json`/`discoveries.json` in `data/output/` were already byte-identical to their published copies — safe to run the full script). `packages/shared-types/src/data/wars.json` verified: byte-identical (md5) to `data/output/wars.json`, 771 entries, matches the live-fetched data above.
